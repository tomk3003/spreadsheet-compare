name: linux
on:
  push:
    branches:
      - '*'
    tags-ignore:
      - '*'
  pull_request:
jobs:
  perl:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        perl-version:
#          - '5.24'
#          - '5.26'
#          - '5.28'
#          - '5.30'
          - '5.32'
    container:
      image: perl:${{ matrix.perl-version }}
    steps:
      - uses: actions/checkout@v2
      - name: perl -V
        run: perl -V
      - name: install dependencies
        #run: cpanm -n -v --no-interactive --installdeps --skip-satisfied .
        run: cpanm -n --no-interactive --installdeps --skip-satisfied .
      - name: Run tests
        env:
          SPREADSHEET_COMPARE_DEBUG: 1
        run: prove -Ilib -m -v --jobs 4
